// Tailwind 4 PostCSS plugin. The legacy `tailwindcss` PostCSS plugin from v3
// is split out into `@tailwindcss/postcss` in v4. See:
// docs/skills/deprecations.md and the Tailwind 4 install guide.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
